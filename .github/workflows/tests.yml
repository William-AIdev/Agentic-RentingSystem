name: tests

on:
  push:
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-test.txt

      - name: Run Order Services Tests with Postgres
        env:
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: |
          pytest tests/test_order_services.py

      - name: Lint
        continue-on-error: true
        run: |
          ruff check .
